{
  "scripts": {
    "start": "node server.js",
    "deploy": "git add . && git commit -m \"Update\" && git push origin master && ssh root@145.223.23.204 \"cd /root/curve-app && git pull && pm2 restart curve-app\""
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "multer": "^2.0.2"
  }
}